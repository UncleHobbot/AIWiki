# LLM Wiki Python dependencies
# Install with: python -m pip install -r requirements.txt

# Web fetching & parsing
requests>=2.28.0
beautifulsoup4>=4.12.0
readability-lxml>=0.8.0

# YouTube transcript extraction
youtube-transcript-api>=0.6.0

# Reddit API (public JSON; optional OAuth via .env)
# praw>=7.0.0  # uncomment if using Reddit OAuth

# Obsidian vault analysis (backlinks, broken links, orphans)
obsidiantools>=0.11.0
networkx>=3.0
pandas>=2.0.0
python-frontmatter>=1.0.0
PyYAML>=6.0
Markdown>=3.4.0

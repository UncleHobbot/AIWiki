Kind: captions Language: en Coding agents have one weakness that Coding agents have one weakness that Coding agents have one weakness that nobody really talks about. They read nobody really talks about. They read nobody really talks about. They read your code file by file. So, when you ask your code file by file. So, when you ask your code file by file. So, when you ask Cursor or Open Code or Claude Code to Cursor or Open Code or Claude Code to Cursor or Open Code or Claude Code to change one function, the agent looks at change one function, the agent looks at change one function, the agent looks at that function, edits it, and moves on. that function, edits it, and moves on. that function, edits it, and moves on. It doesn't know that 40 other places in It doesn't know that 40 other places in It doesn't know that 40 other places in the code base depend on what that the code base depend on what that the code base depend on what that function returns. It doesn't know which function returns. It doesn't know which function returns. It doesn't know which class extends which or which import class extends which or which import class extends which or which import chain breaks when you rename a method. chain breaks when you rename a method. chain breaks when you rename a method. The change looks clean in the diff, you The change looks clean in the diff, you The change looks clean in the diff, you approve it, and 3 days later, something approve it, and 3 days later, something approve it, and 3 days later, something silently fails in production. This silently fails in production. This silently fails in production. This happens because the agent has no map of happens because the agent has no map of happens because the agent has no map of your code base. It only sees what's in your code base. It only sees what's in your code base. It only sees what's in front of it at that moment. Git Nexus is front of it at that moment. Git Nexus is front of it at that moment. Git Nexus is the fix for that blindness. the fix for that blindness. the fix for that blindness. This is the complete nervous system for This is the complete nervous system for This is the complete nervous system for agent context. The tool scans your agent context. The tool scans your agent context. The tool scans your entire code base and builds a knowledge entire code base and builds a knowledge entire code base and builds a knowledge graph out of it. Every function call, graph out of it. Every function call, graph out of it. Every function call, every import, every inheritance chain, every import, every inheritance chain, every import, every inheritance chain, every execution flow from entry point every execution flow from entry point every execution flow from entry point all the way to the leaf nodes. all the way to the leaf nodes. all the way to the leaf nodes. All of that gets stored in a real graph All of that gets stored in a real graph All of that gets stored in a real graph database on your machine. Then it database on your machine. Then it database on your machine. Then it exposes that graph through MCP, so your exposes that graph through MCP, so your exposes that graph through MCP, so your agent can ask actual architectural agent can ask actual architectural agent can ask actual architectural questions before making any change. questions before making any change. questions before making any change. Instead of grepping and guessing, it Instead of grepping and guessing, it Instead of grepping and guessing, it queries the graph and gets the truth. queries the graph and gets the truth. queries the graph and gets the truth. So, when you ask your agent to refactor So, when you ask your agent to refactor So, when you ask your agent to refactor anything, it actually knows what calls anything, it actually knows what calls anything, it actually knows what calls what, what extends what, and what will what, what extends what, and what will what, what extends what, and what will break before it touches a line. break before it touches a line. break before it touches a line. The repo has somewhere around 36,000 The repo has somewhere around 36,000 The repo has somewhere around 36,000 stars on GitHub, which tells people are stars on GitHub, which tells people are stars on GitHub, which tells people are finding it useful. And it supports 14 finding it useful. And it supports 14 finding it useful. And it supports 14 languages, including TypeScript, languages, including TypeScript, languages, including TypeScript, JavaScript, Python, Java, Kotlin, C#, JavaScript, Python, Java, Kotlin, C#, JavaScript, Python, Java, Kotlin, C#, Go, Rust, PHP, Ruby, Swift, C, C++, and Go, Rust, PHP, Ruby, Swift, C, C++, and Go, Rust, PHP, Ruby, Swift, C, C++, and Dart. So, whatever stack you're on, Dart. So, whatever stack you're on, Dart. So, whatever stack you're on, there's a good chance it works out of there's a good chance it works out of there's a good chance it works out of the box. Now, before we get into the box. Now, before we get into the box. Now, before we get into install, I want to quickly compare this install, I want to quickly compare this install, I want to quickly compare this to the Graphify skill we did earlier to the Graphify skill we did earlier to the Graphify skill we did earlier because they look similar on the because they look similar on the because they look similar on the surface, but they solve different surface, but they solve different surface, but they solve different problems. Graphify is a Claude skill. It problems. Graphify is a Claude skill. It problems. Graphify is a Claude skill. It runs inside Claude itself. It makes a runs inside Claude itself. It makes a runs inside Claude itself. It makes a mental graph of your code, and that mental graph of your code, and that mental graph of your code, and that graph lives in the conversation. It's graph lives in the conversation. It's graph lives in the conversation. It's good for one-off exploration, but it good for one-off exploration, but it good for one-off exploration, but it doesn't persist, and it doesn't expose doesn't persist, and it doesn't expose doesn't persist, and it doesn't expose tools to other agents. tools to other agents. tools to other agents. Git Nexus goes deeper. It actually Git Nexus goes deeper. It actually Git Nexus goes deeper. It actually indexes your repo into a real graph indexes your repo into a real graph indexes your repo into a real graph database called Ladybug DB. It stores it database called Ladybug DB. It stores it database called Ladybug DB. It stores it on disk in a .git nexus folder, and it on disk in a .git nexus folder, and it on disk in a .git nexus folder, and it serves that graph over MCP to any agent serves that graph over MCP to any agent serves that graph over MCP to any agent that supports MCP. So, Cursor, Claude that supports MCP. So, Cursor, Claude that supports MCP. So, Cursor, Claude Code, Anti-Gravity, Codex, Windsurf, Code, Anti-Gravity, Codex, Windsurf, Code, Anti-Gravity, Codex, Windsurf, Open Code, all of them can connect to Open Code, all of them can connect to Open Code, all of them can connect to the same index. And because the graph is the same index. And because the graph is the same index. And because the graph is precomputed, you get a lot more accuracy precomputed, you get a lot more accuracy precomputed, you get a lot more accuracy on things like impact analysis or rename on things like impact analysis or rename on things like impact analysis or rename refactors. refactors. refactors. Graphify is lightweight. Git Nexus is Graphify is lightweight. Git Nexus is Graphify is lightweight. Git Nexus is industrial. Different tools, different industrial. Different tools, different industrial. Different tools, different jobs. Okay, let's actually install it. jobs. Okay, let's actually install it. jobs. Okay, let's actually install it. Open your terminal, go to the root of Open your terminal, go to the root of Open your terminal, go to the root of your project, and run NPX Git Nexus your project, and run NPX Git Nexus your project, and run NPX Git Nexus analyze. If your project is not a Git analyze. If your project is not a Git analyze. If your project is not a Git repo, you add the flag {dash} {dash} repo, you add the flag {dash} {dash} repo, you add the flag {dash} {dash} skip git. That's the only thing you need skip git. That's the only thing you need skip git. That's the only thing you need to know to start. This one command does to know to start. This one command does to know to start. This one command does a lot under the hood. It walks the file a lot under the hood. It walks the file a lot under the hood. It walks the file tree, parses every file with tree, parses every file with tree, parses every file with tree-sitter, extracts all the functions tree-sitter, extracts all the functions tree-sitter, extracts all the functions and classes and methods, resolves the and classes and methods, resolves the and classes and methods, resolves the imports across files, figures out which imports across files, figures out which imports across files, figures out which classes inherit from which, runs classes inherit from which, runs classes inherit from which, runs community detection to group related community detection to group related community detection to group related code into clusters, traces the execution code into clusters, traces the execution code into clusters, traces the execution flows from entry points, and then builds flows from entry points, and then builds flows from entry points, and then builds a hybrid search index on top using BM25 a hybrid search index on top using BM25 a hybrid search index on top using BM25 plus semantic embeddings. All local, no plus semantic embeddings. All local, no plus semantic embeddings. All local, no network calls, nothing leaves your network calls, nothing leaves your network calls, nothing leaves your machine. When it finishes, you'll see it machine. When it finishes, you'll see it machine. When it finishes, you'll see it created a few new files in your project. created a few new files in your project. created a few new files in your project. There's an agents.md file, a claud.md There's an agents.md file, a claud.md There's an agents.md file, a claud.md file, and a folder called .claud/skills file, and a folder called .claud/skills file, and a folder called .claud/skills with four skill files inside. The skills with four skill files inside. The skills with four skill files inside. The skills are named exploring, debugging, impact are named exploring, debugging, impact are named exploring, debugging, impact analysis, and refactoring. analysis, and refactoring. analysis, and refactoring. Each one is a markdown file that tells Each one is a markdown file that tells Each one is a markdown file that tells your agent how to use the Git Nexus your agent how to use the Git Nexus your agent how to use the Git Nexus tools for that specific task. So, when tools for that specific task. So, when tools for that specific task. So, when you ask the agent to debug something, it you ask the agent to debug something, it you ask the agent to debug something, it knows to call the context tool to get knows to call the context tool to get knows to call the context tool to get the 360° view of the symbol. When you the 360° view of the symbol. When you the 360° view of the symbol. When you ask it to refactor, it knows to run ask it to refactor, it knows to run ask it to refactor, it knows to run impact analysis first. impact analysis first. impact analysis first. It also regenerate the agents.do.md file It also regenerate the agents.do.md file It also regenerate the agents.do.md file and added instructions that says, "Use and added instructions that says, "Use and added instructions that says, "Use the Git Nexus MCP tools to understand the Git Nexus MCP tools to understand the Git Nexus MCP tools to understand code every time." This is the nudge your code every time." This is the nudge your code every time." This is the nudge your agent needs to actually reach for the agent needs to actually reach for the agent needs to actually reach for the graph instead of falling back to grep or graph instead of falling back to grep or graph instead of falling back to grep or read file. Without that line, sometimes read file. Without that line, sometimes read file. Without that line, sometimes the agent forgets the tools are even the agent forgets the tools are even the agent forgets the tools are even there. there. there. Next part is hooking it into Open Code. Next part is hooking it into Open Code. Next part is hooking it into Open Code. You open the config file for Open Code, You open the config file for Open Code, You open the config file for Open Code, and you add an MCP block. Inside the MCP and you add an MCP block. Inside the MCP and you add an MCP block. Inside the MCP block, you give it a key called Git block, you give it a key called Git block, you give it a key called Git Nexus. You set the type to local, and Nexus. You set the type to local, and Nexus. You set the type to local, and the command is just Git Nexus MCP. Save the command is just Git Nexus MCP. Save the command is just Git Nexus MCP. Save the file, restart Open Code, and now the file, restart Open Code, and now the file, restart Open Code, and now your agent has access to all the Git your agent has access to all the Git your agent has access to all the Git Nexus tools. Also, there is a command Nexus tools. Also, there is a command Nexus tools. Also, there is a command called Git Nexus setup. If you run this called Git Nexus setup. If you run this called Git Nexus setup. If you run this command, it will set up the MCP server command, it will set up the MCP server command, it will set up the MCP server and skill for all the available agent on and skill for all the available agent on and skill for all the available agent on your system. You don't have to manually your system. You don't have to manually your system. You don't have to manually set up the MCP and skills for each agent set up the MCP and skills for each agent set up the MCP and skills for each agent one by one. There are 11 tools per repo one by one. There are 11 tools per repo one by one. There are 11 tools per repo and five more for repository groups. The and five more for repository groups. The and five more for repository groups. The big ones are query, which does process big ones are query, which does process big ones are query, which does process grouped hybrid search. Context, which grouped hybrid search. Context, which grouped hybrid search. Context, which gives you a complete view of any symbol gives you a complete view of any symbol gives you a complete view of any symbol with all its callers and callees. with all its callers and callees. with all its callers and callees. Impact, which is the blast radius Impact, which is the blast radius Impact, which is the blast radius analysis, so you can ask what breaks if analysis, so you can ask what breaks if analysis, so you can ask what breaks if I change this function and get a real I change this function and get a real I change this function and get a real answer with confidence scores. Detect answer with confidence scores. Detect answer with confidence scores. Detect changes, which maps a Git diff to all changes, which maps a Git diff to all changes, which maps a Git diff to all the affected processes. Rename, which the affected processes. Rename, which the affected processes. Rename, which does multi-file coordinated renames does multi-file coordinated renames does multi-file coordinated renames using the graph plus text search. And using the graph plus text search. And using the graph plus text search. And Cypher, which lets the agent run raw Cypher, which lets the agent run raw Cypher, which lets the agent run raw graph queries when it needs something graph queries when it needs something graph queries when it needs something custom. custom. custom. This is genuinely the part that changes This is genuinely the part that changes This is genuinely the part that changes how your agent works. Most agents fail how your agent works. Most agents fail how your agent works. Most agents fail on big refactors because they only see on big refactors because they only see on big refactors because they only see the file they're editing. With these the file they're editing. With these the file they're editing. With these tools, they can ask the graph first, tools, they can ask the graph first, tools, they can ask the graph first, plan the change, and then execute. Even plan the change, and then execute. Even plan the change, and then execute. Even smaller models start performing like the smaller models start performing like the smaller models start performing like the big ones because the heavy lifting is big ones because the heavy lifting is big ones because the heavy lifting is done by the precomputed index, not the done by the precomputed index, not the done by the precomputed index, not the model's reasoning. Now, let me show you model's reasoning. Now, let me show you model's reasoning. Now, let me show you the website of things. the website of things. the website of things. You open gitnexus.vercel.app You open gitnexus.vercel.app You open gitnexus.vercel.app in your browser. By default, it shows a in your browser. By default, it shows a in your browser. By default, it shows a small instruction screen telling you to small instruction screen telling you to small instruction screen telling you to run this NPX command. So, you go back to run this NPX command. So, you go back to run this NPX command. So, you go back to the terminal and run that command. It the terminal and run that command. It the terminal and run that command. It starts a local HTTP server on port 4747. starts a local HTTP server on port 4747. starts a local HTTP server on port 4747. The browser is listening for it, and as The browser is listening for it, and as The browser is listening for it, and as soon as the server comes online, the soon as the server comes online, the soon as the server comes online, the page auto connects. No login, no upload, page auto connects. No login, no upload, page auto connects. No login, no upload, nothing leaves your machine. Once it nothing leaves your machine. Once it nothing leaves your machine. Once it connects, you get this full interactive connects, you get this full interactive connects, you get this full interactive graph of your code base. Files and graph of your code base. Files and graph of your code base. Files and folders on the left sidebar. folders on the left sidebar. folders on the left sidebar. The middle is a force-directed graph The middle is a force-directed graph The middle is a force-directed graph with every node colored by type. Folders with every node colored by type. Folders with every node colored by type. Folders are one color, files another. Classes, are one color, files another. Classes, are one color, files another. Classes, methods, functions, interfaces, all have methods, functions, interfaces, all have methods, functions, interfaces, all have their own colors. You can zoom, pan, their own colors. You can zoom, pan, their own colors. You can zoom, pan, click on any node to see its click on any node to see its click on any node to see its connections. There's a search bar at the connections. There's a search bar at the connections. There's a search bar at the top to find any symbol. And there's a top to find any symbol. And there's a top to find any symbol. And there's a query button at the bottom left where query button at the bottom left where query button at the bottom left where you can write raw Cypher queries against you can write raw Cypher queries against you can write raw Cypher queries against the graph if you want to get specific. the graph if you want to get specific. the graph if you want to get specific. The top right has a settings icon. You The top right has a settings icon. You The top right has a settings icon. You click settings, you'll see a list of click settings, you'll see a list of click settings, you'll see a list of providers. OpenAI, Anthropic, Open providers. OpenAI, Anthropic, Open providers. OpenAI, Anthropic, Open Router, Ollama, all the usual ones. You Router, Ollama, all the usual ones. You Router, Ollama, all the usual ones. You paste in your API key, save it, and now paste in your API key, save it, and now paste in your API key, save it, and now you can chat with the code base directly you can chat with the code base directly you can chat with the code base directly in the browser. The chat panel uses a in the browser. The chat panel uses a in the browser. The chat panel uses a LangChain React agent that has access to LangChain React agent that has access to LangChain React agent that has access to the same MCP tools. the same MCP tools. the same MCP tools. So, you can ask things like, "What does So, you can ask things like, "What does So, you can ask things like, "What does the authentication flow look like?" or the authentication flow look like?" or the authentication flow look like?" or "Which clusters depend on the user "Which clusters depend on the user "Which clusters depend on the user service?" and it'll trace through the service?" and it'll trace through the service?" and it'll trace through the graph and answer with real evidence, not graph and answer with real evidence, not graph and answer with real evidence, not hallucinations. If you don't want to use hallucinations. If you don't want to use hallucinations. If you don't want to use the hosted page, the repo also has the hosted page, the repo also has the hosted page, the repo also has instructions to run the whole thing instructions to run the whole thing instructions to run the whole thing locally. You clone the repo, install locally. You clone the repo, install locally. You clone the repo, install dependencies in the Git Nexus shared dependencies in the Git Nexus shared dependencies in the Git Nexus shared folder, build it, then go into the Git folder, build it, then go into the Git folder, build it, then go into the Git Nexus web folder, install and run npm Nexus web folder, install and run npm Nexus web folder, install and run npm run dev. Then in another terminal, you run dev. Then in another terminal, you run dev. Then in another terminal, you run Git Nexus serve as the back end. run Git Nexus serve as the back end. run Git Nexus serve as the back end. Same UI, fully offline, useful if you're Same UI, fully offline, useful if you're Same UI, fully offline, useful if you're on a corporate network or working with on a corporate network or working with on a corporate network or working with private code that you want extra layers private code that you want extra layers private code that you want extra layers around. There's also a Docker setup if around. There's also a Docker setup if around. There's also a Docker setup if that's more your thing. Two signed that's more your thing. Two signed that's more your thing. Two signed images on GHCR and Docker Hub. One for images on GHCR and Docker Hub. One for images on GHCR and Docker Hub. One for the CLI server and one for the web UI. the CLI server and one for the web UI. the CLI server and one for the web UI. You run docker compose up {dash}d, and You run docker compose up {dash}d, and You run docker compose up {dash}d, and both come up. The images are signed with both come up. The images are signed with both come up. The images are signed with cosign and tied to specific Git tags, cosign and tied to specific Git tags, cosign and tied to specific Git tags, which is honestly a level of supply which is honestly a level of supply which is honestly a level of supply chain security you don't see in most chain security you don't see in most chain security you don't see in most open source tools. open source tools. open source tools. They even ship a Kubernetes cluster They even ship a Kubernetes cluster They even ship a Kubernetes cluster image policy if you want to enforce image policy if you want to enforce image policy if you want to enforce signature verification at admission signature verification at admission signature verification at admission time. For pricing context, the open time. For pricing context, the open time. For pricing context, the open source version is licensed under source version is licensed under source version is licensed under PolyForm non-commercial, so it's free PolyForm non-commercial, so it's free PolyForm non-commercial, so it's free for personal and non-commercial use. for personal and non-commercial use. for personal and non-commercial use. They have an enterprise SaaS at They have an enterprise SaaS at They have an enterprise SaaS at aconlabs.com if you want PR review aconlabs.com if you want PR review aconlabs.com if you want PR review automation, auto reindexing, multi-repo automation, auto reindexing, multi-repo automation, auto reindexing, multi-repo support, and OCaml language coverage. support, and OCaml language coverage. support, and OCaml language coverage. But for solo devs and most teams, the But for solo devs and most teams, the But for solo devs and most teams, the open source version covers everything open source version covers everything open source version covers everything you actually need. One last tip. After you actually need. One last tip. After you actually need. One last tip. After you make commits, the index can go you make commits, the index can go you make commits, the index can go stale. If you're using Claude Code, the stale. If you're using Claude Code, the stale. If you're using Claude Code, the post tool use hook will detect this and post tool use hook will detect this and post tool use hook will detect this and prompt the agent to reindex. For other prompt the agent to reindex. For other prompt the agent to reindex. For other editors, you just run Git Nexus analyze editors, you just run Git Nexus analyze editors, you just run Git Nexus analyze again or Git Nexus analyze {dash} {dash} again or Git Nexus analyze {dash} {dash} again or Git Nexus analyze {dash} {dash} force if you want a clean rebuild. force if you want a clean rebuild. force if you want a clean rebuild. Reindexing on a medium repo takes maybe Reindexing on a medium repo takes maybe Reindexing on a medium repo takes maybe 30 to 60 seconds. That's Git Nexus. 30 to 60 seconds. That's Git Nexus. 30 to 60 seconds. That's Git Nexus. Index your repo once, plug it into Open Index your repo once, plug it into Open Index your repo once, plug it into Open Code through MCP, and your agent stops Code through MCP, and your agent stops Code through MCP, and your agent stops shipping blind edits. But the real value shipping blind edits. But the real value shipping blind edits. But the real value is the MCP server feeding precomputed is the MCP server feeding precomputed is the MCP server feeding precomputed structure to whatever agent you use. Try structure to whatever agent you use. Try structure to whatever agent you use. Try it on a project you know well and see if it on a project you know well and see if it on a project you know well and see if the graph surfaces relationships you the graph surfaces relationships you the graph surfaces relationships you forgot existed. All right, so that's it forgot existed. All right, so that's it forgot existed. All right, so that's it from the video, and I hope you enjoyed from the video, and I hope you enjoyed from the video, and I hope you enjoyed it. If you did, please like this video it. If you did, please like this video it. If you did, please like this video and subscribe to the channel, and I'll and subscribe to the channel, and I'll and subscribe to the channel, and I'll see you in the next video.